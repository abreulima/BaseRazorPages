@page
@model BaseRazorPages.Pages.IndexModel

@{
    Layout = "_Layout";
}

<h1>Index</h1>

@Model.name

@foreach (var user in Model.Users)
{
    <p>Name: @user.Name</p>
    <p>Age: @user.Password</p>
}