@page
@model BaseRazorPages.Pages.IndexModel

@{
    Layout = "_Layout";
}

<h1>Index</h1>

@foreach (var user in Model.Users)
{
    <span>Name: @user.Name</span>
    <span>Password: @user.Password</span>
    <br />
}